#include <stdio.h>

#define max 50

int main (){

 
int arr[max];
int integer;
for (size_t i = 0; i < count; i++)
{
printf("Enter the value for %d element :",i + 1;);
scanf("%d",arr[i];);
}

printf("Enter  value to find array: ");
scanf("%d ",integer);



    return 0;
}